import src


def test_package_imports():
    """Placeholder: proves CI can install deps and import the package.
    Replace with real tests once the judge exists (Phase 3)."""
    assert src.__version__ == "0.1.0"
